node_modules/
__pycache__/
*.py.bak
*.log
.DS_Store

# Playwright smoke test artifacts
test-results/
playwright-report/
.playwright/

# Frontend v2 build tooling artifacts — the built output at dist/public/new/
# IS committed (same pattern as dist/public/assets/) so deploys don't depend
# on Railway's build environment. The Vite dev tooling artifacts below are
# still transient.
frontend/dist/
frontend/.vite/
